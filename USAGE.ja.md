# 使い方ガイド

[English](USAGE.md) | **日本語** | [中文](USAGE.zh.md) | [한국어](USAGE.ko.md) | [Deutsch](USAGE.de.md) | [Español](USAGE.es.md) | [Bahasa Indonesia](USAGE.id.md)

インストールから最初の1枚を生成するまでの流れを、実際の画面つきで説明します。

## 1. インストール

インストーラー（`Qwen-Image_2.1_HACK_Studio_Setup.exe`）を実行し、使用許諾契約に
同意してインストールします。完了後、デスクトップまたはスタートメニューから起動します。

データ（生成画像・アップロード・ジョブ履歴・設定）は
`%LOCALAPPDATA%\Qwen-Image 2.1 HACK Studio\` に保存され、アンインストールや
アップグレードをしても**残ります**。

## 2. ComfyUI バックエンドを登録する

初回起動後、**設定 → ComfyUI サーバー** で、Qwen-Image 2.1 のモデルが導入済みの
ComfyUI を登録します（導入方法は README を参照）。

![ComfyUI バックエンド設定画面](screenshots/backend-setup.jpg)

- **名前**: 任意（例: `Local`）
- **URL**: ComfyUI の URL（同じマシンなら通常 `http://127.0.0.1:8188`）
- **input_dir**（任意）: ComfyUI が同じマシンにある場合のみ入力します。参照画像の
  アップロードがわずかに速くなります。別マシンの ComfyUI なら空欄で構いません

バックエンドは複数登録できます（同じマシンの別 GPU や別マシン）。送信したジョブは
いちばん空いているところへ自動で振り分けられます。

## 3. モデルを確認する

**設定 → モデル** を開くと、各モデルファイルとカスタムノードが、バックエンドごとに
導入済みかどうか一覧で表示されます。

![モデル画面](screenshots/models.jpg)

足りないものには印が付きます。README のセットアップ表を見て ComfyUI に導入し、
**ComfyUI を再起動**してから、この画面を開き直してください。

## 4. （任意）追加機能で高速化を選ぶ

**設定 → 追加機能**（または生成画面の「⚡ 追加機能」ボタン）で、サンプラー、
UNET／テキストエンコーダの種類、Spectrum、SageAttention、出力のトリミング、
スリープ回避を設定できます。生成フォームには出てこない項目で、ここで選んだ内容が
常に使われます。

![追加機能画面](screenshots/extra-features.jpg)

初めてなら既定のままで問題ありません。速度を求めるなら **Spectrum** と
**SageAttention** を ON にしてください。この画面の折りたたみベンチマークに、
全組み合わせの実測結果があります。

- **生成後のトリミング**: 上下をカットして高さ 1080/720/480px ぴったりにします
  （モデル自体は 16 の倍数で出力するため、1080 → 1088 になります）。
- **スリープ回避**: 生成中に小さな音を定期的に鳴らして Windows のスリープを防ぎます。
  音が気になる場合は OFF にしてください。

## 5. 画像を生成する

メイン画面に戻り、タブ（テキスト→画像 / 画像編集）を選んでプロンプトを入力します。

![生成フォーム](screenshots/generation.jpg)

- **サイズ／アスペクト比**: プリセットから選びます（実際に送信されるピクセル数が
  リアルタイムで表示されます）
- **画像編集**タブでは参照画像を1枚以上追加します。大きな画像は、アスペクト比を
  保ったまま約 100 万画素へ自動縮小されます

### 💡 コツ: 画像編集は参照画像2枚がいちばん効く

参照画像が1枚だと、Qwen-Image 2.1 の編集がほとんど効かないことがあります。
本アプリは1枚のとき、グレーのダミー画像を自動で足して補います。それでも変化が
乏しいときは、関連する2枚目の画像を追加してみてください。

入力できたら **生成** をクリックすると、キューに追加されます。

## 6. キューで結果を確認する

送信したジョブはキューに並び、完了するとその場で画像が表示されます。サムネイルを
クリックするとビューワ（ウィンドウいっぱい／フルスクリーン）が開きます。
「フォームに復元」で、過去のジョブの設定をそのまま生成フォームへ戻せます。

## 困ったときは

質問や不具合報告は [Issues](../../issues) へお願いします。
