# 使用指南

[English](USAGE.md) | [日本語](USAGE.ja.md) | **中文** | [한국어](USAGE.ko.md) | [Deutsch](USAGE.de.md) | [Español](USAGE.es.md) | [Bahasa Indonesia](USAGE.id.md)

从安装到生成第一张图像的完整流程，附实际界面截图。

## 1. 安装

运行安装程序（`Qwen-Image_2.1_HACK_Studio_Setup.exe`），同意许可协议后安装。
完成后，可从桌面或开始菜单启动。

您的数据（生成的图像、上传文件、任务历史、设置）保存在
`%LOCALAPPDATA%\Qwen-Image 2.1 HACK Studio\`，卸载或升级时**会保留**。

## 2. 注册 ComfyUI 后端

首次启动后，进入 **设置 → ComfyUI 服务器**，注册一个已安装 Qwen-Image 2.1 模型的
ComfyUI 实例（见 README）。

![ComfyUI 后端设置界面](screenshots/backend-setup.jpg)

- **名称**：任意标签（如 `Local`）
- **URL**：ComfyUI 的地址（同一台机器通常为 `http://127.0.0.1:8188`）
- **input_dir**（可选）：仅当 ComfyUI 在同一台机器上时填写，可略微加快参考图上传。
  ComfyUI 在其他机器上时留空即可

可以注册多个后端（同一台机器的其他 GPU，或其他机器）——每次提交都会自动分配给最空闲的一个。

## 3. 检查模型

打开 **设置 → 模型**。每个模型文件和自定义节点都会列出，并显示各后端是否已安装。

![模型界面](screenshots/models.jpg)

缺少的项目会被标出；请按 README 的安装表将其装入 ComfyUI，**重启 ComfyUI**，然后重新打开此界面。

## 4.（可选）在附加功能中选择加速选项

在 **设置 → 附加功能**（或生成界面的“⚡ 附加功能”按钮）中，可配置采样器、UNET／文本编码器版本、
Spectrum、SageAttention、输出裁剪和防休眠。这些不会显示在生成表单上——这里的选择始终生效。

![附加功能界面](screenshots/extra-features.jpg)

首次使用时默认值即可。追求速度请开启 **Spectrum** 和 **SageAttention**——本界面的可折叠基准报告列出了各种组合的实测效果。

- **生成后裁剪**：裁剪上下，使高度正好为 1080/720/480 px（模型本身按 16 的倍数输出，例如 1080 → 1088）。
- **防休眠**：在任务期间定期播放极小的声音以保持 Windows 唤醒。如果声音打扰您，请关闭。

## 5. 生成图像

回到主界面，选择标签页——文生图或 Edit——并输入提示词。

![生成表单](screenshots/generation.jpg)

- **尺寸／宽高比**：从预设中选择（实时显示实际提交的像素尺寸）
- 在 **Edit** 标签页中添加一张或多张参考图。大图会自动缩小到约 100 万像素，并保持宽高比

### 💡 提示：Edit 使用两张参考图效果最好

只有一张参考图时，Qwen-Image 2.1 Edit 有时几乎没有效果。应用会自动用一张灰色填充图补足单张参考图。
如果结果仍然看起来没有变化，请再添加一张相关的图像。

全部填好后，点击 **生成**，任务会加入队列。

## 6. 在队列中查看结果

提交的任务会出现在队列中；完成后图像会直接显示。点击缩略图打开查看器（全窗口或全屏），
使用“恢复到表单”可将过去任务的完整设置带回表单。

## 需要帮助？

有问题或错误报告请使用 [Issues](../../issues)。
